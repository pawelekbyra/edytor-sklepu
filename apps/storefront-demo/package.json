{
  "name": "@editor/storefront-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Integration spike: a standalone storefront that renders a page document from its own repo (JSON) through @editor/renderer + @editor/component-library — the \"własne repo\" mode from ARCHITEKTURA.md.",
  "type": "module",
  "scripts": {
    "dev": "next dev --port 3200",
    "build": "next build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@editor/component-library": "workspace:*",
    "@editor/persistence": "workspace:*",
    "@editor/renderer": "workspace:*",
    "@editor/schema": "workspace:*",
    "next": "^15.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "typescript": "^5.6.3"
  }
}
