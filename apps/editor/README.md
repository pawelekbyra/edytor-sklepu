# apps/editor

Placeholder — the Next.js editor UI is built starting at stage 6 of
[`docs/NEW_ARCHITECTURE.md`](../../docs/NEW_ARCHITECTURE.md) (canvas + drag and drop), once
`packages/schema`, `packages/editor-core`, `packages/persistence` and `packages/renderer` exist.
