{
  "name": "edytor-sklepu-monorepo",
  "private": true,
  "description": "TypeScript rewrite of the spree_page_builder visual editor (apps/packages) alongside the existing Rails gems.",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
