{
  "name": "@editor/persistence",
  "version": "0.0.1",
  "private": true,
  "description": "Repository interfaces (Page/Theme/Version/Media/Commerce) + a better-sqlite3 demo implementation. The only package that gets swapped for a real API client when integrating with sklepik.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@editor/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
